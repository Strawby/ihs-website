diff --git a/styles.css b/styles.css
index 7ed0fbfeb393e7bae9fb3374f445288c0d4d0c70..b94055158d96fd171675ffe425f8cbded957c519 100644
--- a/styles.css
+++ b/styles.css
@@ -934,50 +934,61 @@ body {
   .footer-hours {
     flex-direction: column;
   }
 
   .footer-section-group::after {
     display: none;
   }
 
   .footer-section + .footer-section {
     border-top: 1px solid var(--footer-border);
     padding-top: 1.25rem;
     margin-top: 0.25rem;
   }
 
   .footer-meta {
     text-align: center;
   }
 }
 
 .pool-page {
   position: relative;
   padding: clamp(3rem, 6vw, 4.5rem) 0 clamp(5rem, 8vw, 6rem);
   display: flex;
   flex-direction: column;
   gap: clamp(2.75rem, 5vw, 3.75rem);
+  line-height: 1.6;
+}
+
+.pool-page p {
+  font-size: 0.95rem;
+}
+
+.pool-page small,
+.pool-page .note {
+  font-size: 0.8rem;
+  color: var(--muted);
 }
 
 .pool-main {
   padding-top: clamp(6.5rem, 11vw, 9.5rem);
   max-width: min(1180px, 100% - 2.5rem);
   margin: 0 auto;
   width: 100%;
 }
 
 .pool-hero {
   max-width: min(1180px, 100% - 2.5rem);
   width: 100%;
   margin: 0 auto clamp(2.5rem, 5vw, 3.75rem);
 }
 
 .pool-hero-inner {
   display: grid;
   grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
   align-items: center;
   gap: clamp(1.5rem, 4vw, 3rem);
 }
 
 .pool-hero-content {
   display: grid;
   gap: 0.75rem;
