diff --git a/styles.css b/styles.css
index f192690a80d9ffc3afcf2291fe920d98f7c1aa7a..aff4ffadcb8335fae7fedda8adc00ca888171f8f 100644
--- a/styles.css
+++ b/styles.css
@@ -105,72 +105,63 @@ body {
   padding: .5rem .75rem;
   border-radius: .75rem;
   cursor: pointer;
   margin-left: 1.25rem;
 }
 .nav button:focus-visible { outline: none; box-shadow: var(--ring); }
 
 @media (max-width: 900px) {
   .nav {
     grid-template-columns: auto;
     justify-items: center;
     row-gap: 1rem;
   }
 
   .nav-links,
   .nav-links--right {
     justify-content: center;
     flex-wrap: wrap;
     gap: 1rem;
     width: 100%;
   }
 
   .brand {
     margin: 0;
   }
-  
+
   .hero {
     margin: 0 calc(50% - 50vw) 2rem;
     padding: clamp(2rem, 10vw, 3.5rem) clamp(1rem, 7vw, 2.5rem);
     min-height: max(480px, 100vh);
     gap: clamp(2.5rem, 12vw, 4rem);
   }
 
   .hero-panels {
     margin-top: clamp(3rem, 14vh, 6rem);
     padding: 0 clamp(0.5rem, 4vw, 1.5rem);
     max-width: none;
   }
-  
-  .hero-panel--feature,
-  .hero-panel--with-media {
-    grid-template-columns: 1fr;
-  }
-
-  .hero-panel--feature .feature-media {
-    max-height: 320px;
-  }
 }
 
 .hero {
   position: relative;
   margin: 0 calc(50% - 50vw) 3rem;
   padding: clamp(3rem, 6vw, 5rem) clamp(1.5rem, 8vw, 6rem);
   display: flex;
   flex-direction: column;
   align-items: center;
   justify-content: center;
   gap: clamp(2.5rem, 7vh, 4.5rem);
   text-align: center;
   min-height: max(540px, 100vh);
   overflow: hidden;
   isolation: isolate;
 }
 .hero-media {
   position: fixed;
   inset: 0;
   width: 100vw;
   height: 100vh;
   z-index: -2;
   pointer-events: none;
 }
 .page-filter {
@@ -278,38 +269,56 @@ body {
   padding: .65rem 1.8rem;
   border-radius: 999px;
   font-weight: 700;
   letter-spacing: .08em;
   text-transform: uppercase;
   box-shadow: 0 12px 30px rgba(0,0,0,.2);
   transition: transform .2s ease, box-shadow .2s ease;
 }
 .panel-cta:hover,
 .panel-cta:focus-visible {
   transform: translateY(-2px);
   box-shadow: 0 16px 32px rgba(0,0,0,.28), 0 0 0 3px rgba(25, 94, 82, .45);
   outline: none;
 }
 :root.light .panel-cta:hover,
 :root.light .panel-cta:focus-visible {
   box-shadow: 0 16px 32px rgba(0,0,0,.28), 0 0 0 3px rgba(244, 250, 247, .45);
 }
 :root.light .hero-panel {
   background: linear-gradient(145deg, #28a989 0%, #1f7f6b 100%);
   color: #f7fbf8;
 }
 :root.light .hero-panel p {
   color: rgba(247, 251, 248, .85);
 }
-:root.light .panel-cta {
-  background: #195e52;
-  color: #f4faf7;
+:root.light .panel-cta { 
+  background: #195e52; 
+  color: #f4faf7; 
+}
+
+@media (max-width: 900px) {
+  .hero-panel--feature,
+  .hero-panel--with-media {
+    grid-template-columns: 1fr;
+  }
+
+  .hero-panel--feature .feature-media,
+  .hero-panel--with-media .panel-media {
+    max-height: 320px;
+  }
+
+  .hero-panel--feature .feature-content,
+  .hero-panel--with-media .panel-content {
+    text-align: left;
+    width: 100%;
+  }
 }
 .cards { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1rem; margin-top: 2rem; }
 .card {
   background: var(--card); border: 1px solid #22304a; border-radius: 1rem; padding: 1rem;
   transition: transform .15s ease, box-shadow .15s ease;
 }
 .card:hover { transform: translateY(-2px); box-shadow: 0 6px 24px rgba(0,0,0,.15); }
 .card h3 { margin: 0 0 .25rem; font-size: 1.05rem; }
 .card p { margin: 0; color: var(--muted); }
 .site-footer { text-align: center; padding: 2rem; color: var(--muted); }
