diff --git a/styles.css b/styles.css
index 156defddf57d333b9cc8f718732bfcc3a0e005bc..a8325c2cd2bd5a732fa80f1aa05116d0fbf44120 100644
--- a/styles.css
+++ b/styles.css
@@ -333,80 +333,85 @@ body {
   gap: 1rem;
 }
 
 .simple-page p {
   color: var(--muted);
 }
 
 .simple-page a {
   color: var(--brand);
   font-weight: 600;
 }
 
 @media (max-width: 1300px) {
   :root {
     --header-height: 210px;
   }
   .nav {
     flex-direction: column;
     align-items: stretch;
     padding: 1rem 1.25rem;
     row-gap: .5rem;
     position: relative;
   }
 
   .nav-top {
-    display: flex;
+    display: grid;
+    grid-template-columns: 1fr auto 1fr;
+    align-items: center;
   }
 
   .nav-inner {
     grid-template-columns: 1fr;
     justify-items: center;
     row-gap: 1rem;
     max-width: none;
   }
 
   .nav.nav--collapsible .nav-inner {
     display: none;
     width: 100%;
   }
 
   .nav.nav--collapsible.nav--open .nav-inner {
     display: grid;
     justify-items: start;
     text-align: left;
     align-items: start;
   }
 
   .nav-hamburger {
     display: inline-flex;
     align-self: center;
+    justify-self: end;
   }
 
   .nav-brand--mobile {
     display: flex;
     align-self: center;
+    grid-column: 2;
+    justify-self: center;
   }
 
   .nav-inner > .brand {
     display: none;
   }
 
   .nav.nav--collapsible .nav-links,
   .nav.nav--collapsible .nav-links--right {
     justify-content: flex-start;
   }
 
   .nav-links,
   .nav-links--right {
     justify-content: center;
     flex-direction: column;
     align-items: stretch;
     gap: 1rem;
     width: 100%;
   }
 
   .nav.nav--collapsible .nav-link,
   .nav.nav--collapsible .nav-toggle {
     justify-content: flex-start;
     width: 100%;
   }
