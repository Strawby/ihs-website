diff --git a/styles.css b/styles.css
index 46f4a86b55e29a11cbdb5180119b339340a153bc..646c5f7a0a10d7b9ab35f3b90137038c222c0caf 100644
--- a/styles.css
+++ b/styles.css
@@ -423,72 +423,86 @@ body {
   }
 }
 
 .hero {
   position: relative;
   margin: 0 calc(50% - 50vw) 3rem;
   padding: clamp(3rem, 6vw, 5rem) clamp(1.5rem, 8vw, 6rem);
   display: flex;
   flex-direction: column;
   align-items: center;
   justify-content: center;
   gap: clamp(2.5rem, 7vh, 4.5rem);
   text-align: center;
   min-height: max(540px, 100vh);
   overflow: hidden;
   isolation: isolate;
 }
 
 .hero-media {
   position: fixed;
   inset: 0;
   width: 100vw;
   height: 100vh;
   z-index: -2;
   pointer-events: none;
+  overflow: hidden;
+}
+
+.hero-media img,
+.hero-media video {
+  position: absolute;
+  inset: 0;
+  width: 100%;
+  height: 100%;
+  object-fit: cover;
 }
 
 .page-filter {
   position: fixed;
   inset: 0;
   background: linear-gradient(160deg, rgba(0, 0, 0, .55) 0%, rgba(0, 0, 0, .9) 85%);
   pointer-events: none;
   z-index: -1;
 }
 
 :root.light .page-filter {
   background: linear-gradient(160deg, rgba(15, 23, 42, .35) 0%, rgba(15, 23, 42, .6) 85%);
 }
 
 .hero-video {
   display: block;
-  width: 100%;
-  height: 100%;
-  object-fit: cover;
   filter: blur(var(--hero-blur));
   transform: scale(1.03);
   will-change: filter;
+  z-index: 1;
+}
+
+.hero-image {
+  display: block;
+  filter: blur(var(--hero-blur));
+  transform: scale(1.03);
 }
 
 .hero-panels {
   position: relative;
   display: flex;
   flex-direction: column;
   align-items: center;
   gap: clamp(1.5rem, 3vw, 2.75rem);
   width: 100%;
   max-width: min(960px, 100%);
   margin-top: clamp(4rem, 15vh, 8rem);
   z-index: 1;
 }
 
 .hero-panel {
   background: linear-gradient(145deg, #1f8f73 0%, #1a6f62 100%);
   color: #f2f8f4;
   padding: clamp(1.75rem, 3vw, 2.5rem);
   border-radius: 1.5rem;
   box-shadow: 0 18px 45px rgba(0,0,0,.25);
   display: flex;
   flex-direction: column;
   gap: 1rem;
   align-items: flex-start;
   text-align: left;
