diff --git a/styles.css b/styles.css
index 646c5f7a0a10d7b9ab35f3b90137038c222c0caf..cc4f1a8c06298e031192ef376c1cdc9e6ba4dba5 100644
--- a/styles.css
+++ b/styles.css
@@ -435,50 +435,54 @@ body {
   text-align: center;
   min-height: max(540px, 100vh);
   overflow: hidden;
   isolation: isolate;
 }
 
 .hero-media {
   position: fixed;
   inset: 0;
   width: 100vw;
   height: 100vh;
   z-index: -2;
   pointer-events: none;
   overflow: hidden;
 }
 
 .hero-media img,
 .hero-media video {
   position: absolute;
   inset: 0;
   width: 100%;
   height: 100%;
   object-fit: cover;
 }
 
+.hero-media--fallback .hero-video {
+  display: none;
+}
+
 .page-filter {
   position: fixed;
   inset: 0;
   background: linear-gradient(160deg, rgba(0, 0, 0, .55) 0%, rgba(0, 0, 0, .9) 85%);
   pointer-events: none;
   z-index: -1;
 }
 
 :root.light .page-filter {
   background: linear-gradient(160deg, rgba(15, 23, 42, .35) 0%, rgba(15, 23, 42, .6) 85%);
 }
 
 .hero-video {
   display: block;
   filter: blur(var(--hero-blur));
   transform: scale(1.03);
   will-change: filter;
   z-index: 1;
 }
 
 .hero-image {
   display: block;
   filter: blur(var(--hero-blur));
   transform: scale(1.03);
 }
@@ -889,50 +893,64 @@ body {
   flex-direction: column;
   gap: 2.5rem;
 }
 
 .pool-hero {
   display: grid;
   gap: 0.75rem;
   max-width: 920px;
 }
 
 .pool-kicker {
   text-transform: uppercase;
   letter-spacing: 0.16em;
   font-weight: 700;
   color: var(--brand);
   font-size: 0.85rem;
 }
 
 .pool-lede {
   display: grid;
   gap: 0.75rem;
   color: var(--muted);
   line-height: 1.7;
 }
 
+.pool-hero-figure {
+  margin: 0;
+  border-radius: 1rem;
+  overflow: hidden;
+  box-shadow: 0 18px 48px rgba(0, 0, 0, 0.3);
+}
+
+.pool-hero-figure img {
+  width: 100%;
+  height: 100%;
+  display: block;
+  object-fit: cover;
+}
+
 .pool-section {
   background: var(--card);
   border: 1px solid #22304a;
   border-radius: 1.25rem;
   padding: clamp(1.5rem, 3vw, 2.25rem);
   display: grid;
   gap: 1.25rem;
   box-shadow: 0 16px 36px rgba(0,0,0,.2);
 }
 
 .section-header {
   display: grid;
   gap: 0.25rem;
 }
 
 .section-kicker {
   text-transform: uppercase;
   letter-spacing: 0.1em;
   font-weight: 700;
   color: var(--brand);
   font-size: 0.9rem;
   margin: 0;
 }
 
 .section-summary {
