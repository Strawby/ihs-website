diff --git a/styles.css b/styles.css
index 344cc8d937b8a192b53208f06a1286c8afe9a375..d2fcc512ee6d5e7f17b28f530776738b697b0b00 100644
--- a/styles.css
+++ b/styles.css
@@ -91,104 +91,110 @@ body {
   overflow: hidden;
   clip: rect(0, 0, 0, 0);
   white-space: nowrap;
   border: 0;
 }
 
 .nav {
   display: flex;
   align-items: center;
   gap: 1rem;
   padding: .75rem clamp(1.25rem, 4vw, 2.5rem);
   width: 100%;
   max-width: 1240px;
   margin: 0 auto;
 }
 
 .nav-top {
   display: none;
   align-items: center;
   justify-content: space-between;
   gap: 1rem;
   width: 100%;
 }
 
 .nav-inner {
+  position: relative;
   display: grid;
-  grid-template-columns: 1fr auto 1fr;
+  grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
   align-items: center;
   gap: 1.25rem;
   width: 100%;
   max-width: 1120px;
   margin: 0 auto;
-  justify-items: center;
+}
+
+.nav-links {
+  display: flex;
+  align-items: center;
+  justify-content: flex-end;
+  gap: 1.75rem;
+  text-transform: uppercase;
+  letter-spacing: 0.12em;
+  font-weight: 600;
+  color: var(--fg);
+  min-width: 0;
+  flex: 1;
 }
 
 .nav-hamburger {
   display: none;
   flex-direction: column;
   gap: 6px;
   padding: .5rem .6rem;
   background: var(--card);
   color: var(--fg);
   border-radius: .75rem;
   border: 1px solid #22304a;
   cursor: pointer;
   transition: background .2s ease, color .2s ease, border-color .2s ease;
 }
 
 .nav-hamburger span[aria-hidden="true"] {
   display: block;
   width: 22px;
   height: 2px;
   background: currentColor;
   border-radius: 999px;
   transition: transform .2s ease, opacity .2s ease;
 }
 
 .nav.nav--open .nav-hamburger span:nth-of-type(1) {
   transform: translateY(8px) rotate(45deg);
 }
 
 .nav.nav--open .nav-hamburger span:nth-of-type(2) {
   opacity: 0;
 }
 
 .nav.nav--open .nav-hamburger span:nth-of-type(3) {
   transform: translateY(-8px) rotate(-45deg);
 }
 
-.nav-links {
-  display: flex;
-  align-items: center;
-  justify-content: flex-end;
-  gap: 1.75rem;
-  text-transform: uppercase;
-  letter-spacing: 0.12em;
-  font-weight: 600;
-  color: var(--fg);
-  min-width: 0;
+
+.nav-inner > .brand {
+  justify-self: center;
 }
 
 .nav-links--right {
   justify-content: flex-start;
 }
 
 .nav-link,
 .nav-toggle {
   color: inherit;
   text-decoration: none;
   padding: 0 0 .25rem;
   border-bottom: 2px solid transparent;
   transition: border-color .2s ease, color .2s ease, background .2s ease;
   background: transparent;
   border: none;
   text-transform: inherit;
   letter-spacing: inherit;
   font-weight: inherit;
   font-size: inherit;
   display: inline-flex;
   align-items: center;
   gap: .35rem;
   cursor: pointer;
 }
 
@@ -344,50 +350,51 @@ body {
 
 .simple-page a {
   color: var(--brand);
   font-weight: 600;
 }
 
 @media (max-width: 1300px) {
   :root {
     --header-height: 210px;
   }
   .nav {
     flex-direction: column;
     align-items: stretch;
     padding: 1rem 1.25rem;
     row-gap: .5rem;
     position: relative;
   }
 
   .nav-top {
     display: grid;
     grid-template-columns: 1fr auto 1fr;
     align-items: center;
   }
 
   .nav-inner {
+    display: grid;
     grid-template-columns: 1fr;
     justify-items: center;
     row-gap: 1rem;
     max-width: none;
   }
 
   .nav.nav--collapsible .nav-inner {
     display: none;
     width: 100%;
   }
 
   .nav.nav--collapsible.nav--open .nav-inner {
     display: grid;
     justify-items: start;
     text-align: left;
     align-items: start;
   }
 
   .nav-hamburger {
     display: inline-flex;
     align-self: center;
     justify-self: end;
   }
 
   .nav-brand--mobile {
