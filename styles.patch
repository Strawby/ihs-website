diff --git a/styles.css b/styles.css
index 5ccb1b32c5fd8c4eb32a8fc8998ddf0fb5cf5abe..7e8b599cc2ce8e4b85553ecbc54bdfd03cdf32cb 100644
--- a/styles.css
+++ b/styles.css
@@ -364,55 +364,62 @@ body {
     align-items: start;
   }
 
   .nav-hamburger {
     display: inline-flex;
     align-self: center;
   }
 
   .nav-brand--mobile {
     display: flex;
     align-self: center;
   }
 
   .nav-inner > .brand {
     display: none;
   }
   
   .nav.nav--collapsible .nav-links,
   .nav.nav--collapsible .nav-links--right {
     justify-content: flex-start;
   }
 
   .nav-links,
   .nav-links--right {
     justify-content: center;
-    flex-wrap: wrap;
+    flex-direction: column;
+    align-items: stretch;
     gap: 1rem;
     width: 100%;
   }
 
+  .nav.nav--collapsible .nav-link,
+  .nav.nav--collapsible .nav-toggle {
+    justify-content: flex-start;
+    width: 100%;
+  }
+
   .nav-item,
   .nav-item--nested {
     width: 100%;
   }
 
   .nav-menu {
     position: static;
     width: 100%;
     margin-top: .5rem;
     box-shadow: none;
     padding: .5rem 0;
     background: transparent;
   }
 
   .brand {
     margin: 0;
   }
 
   .hero {
     margin: 0 calc(50% - 50vw) 2rem;
     padding: clamp(2rem, 10vw, 3.5rem) clamp(1rem, 7vw, 2.5rem);
     min-height: max(480px, 100vh);
     gap: clamp(2.5rem, 12vw, 4rem);
   }
 
