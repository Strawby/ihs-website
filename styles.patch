diff --git a/styles.css b/styles.css
index b94055158d96fd171675ffe425f8cbded957c519..8b78f7894c108e1d39db0a5cad2748b5475f5e03 100644
--- a/styles.css
+++ b/styles.css
@@ -1153,59 +1153,70 @@ body {
   grid-template-columns: repeat(2, minmax(0, 1fr));
   gap: clamp(1.25rem, 3vw, 1.75rem);
   align-items: start;
 }
 
 .section-header {
   display: grid;
   gap: 0.25rem;
 }
 
 .section-kicker {
   text-transform: uppercase;
   letter-spacing: 0.1em;
   font-weight: 700;
   color: var(--brand);
   font-size: 0.9rem;
   margin: 0;
 }
 
 .section-summary {
   margin: 0;
   color: var(--muted);
 }
 
 .pool-list {
+  list-style: none;
+  padding: 0;
   margin: 0;
-  padding-left: 1.1rem;
-  display: grid;
-  gap: 0.6rem;
   color: var(--muted);
 }
 
+.pool-list li + li {
+  margin-top: 0.75rem;
+  padding-top: 0.75rem;
+  border-top: 1px solid rgba(148, 163, 184, 0.25);
+}
+
 .pool-list--tight {
-  gap: 0.35rem;
+  /* Slightly reduce spacing for compact lists */
+  --pool-list-tight-spacing: 0.5rem;
+}
+
+.pool-list--tight li + li {
+  margin-top: var(--pool-list-tight-spacing);
+  padding-top: var(--pool-list-tight-spacing);
 }
 
 .pool-grid {
   grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
   gap: clamp(1.1rem, 3vw, 1.6rem);
 }
 
 .pool-card {
   background: color-mix(in srgb, var(--card) 94%, var(--bg));
   border: 1px solid rgba(148, 163, 184, 0.35);
   border-radius: 1rem;
   padding: clamp(1.35rem, 3vw, 1.9rem);
   box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.03), 0 10px 28px rgba(15, 23, 42, 0.18);
   height: 100%;
 }
 
 .info-card {
   background: color-mix(in srgb, var(--card) 96%, var(--bg));
   border: 1px solid rgba(148, 163, 184, 0.3);
   border-radius: 1rem;
   padding: clamp(1.25rem, 2.5vw, 1.6rem);
   display: grid;
   gap: 0.75rem;
   box-shadow: inset 0 1px 0 rgba(255,255,255,.02), 0 8px 22px rgba(15,23,42,.18);
 }
