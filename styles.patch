diff --git a/styles.css b/styles.css
index 43974e6787338c0f66de24737a2853cc89ed733c..ceb6aec4e131d995e5e7bfb305ac69e2b920604a 100644
--- a/styles.css
+++ b/styles.css
@@ -425,147 +425,143 @@ body {
     display: inline-flex;
     margin-left: 0;
   }
 
   .nav-item,
   .nav-item--nested {
     width: 100%;
   }
 
   .nav-menu {
     position: static;
     width: 100%;
     margin-top: .5rem;
     box-shadow: none;
     padding: .5rem 0;
     background: transparent;
   }
 
   .brand {
     margin: 0;
   }
 }
 
 @media (max-width: 900px) {
   .hero {
-    margin: 0 calc(50% - 50vw) 2rem;
-    padding: clamp(2rem, 10vw, 3.5rem) clamp(1rem, 7vw, 2.5rem);
-    min-height: max(480px, 100vh);
-    gap: clamp(2.5rem, 12vw, 4rem);
+    margin: 0 calc(50% - 50vw) 1.5rem;
+    padding: clamp(1.75rem, 9vw, 3rem) clamp(1rem, 7vw, 2.5rem);
+    min-height: max(460px, 90vh);
+    gap: clamp(2rem, 10vw, 3.25rem);
   }
 
   .hero-panels {
-    margin-top: clamp(3rem, 14vh, 6rem);
+    margin-top: clamp(2.25rem, 10vh, 4.5rem);
     padding: 0 clamp(0.5rem, 4vw, 1.5rem);
     max-width: none;
   }
 }
 
 .hero {
   position: relative;
-  margin: 0 calc(50% - 50vw) 3rem;
-  padding: clamp(3rem, 6vw, 5rem) clamp(1.5rem, 8vw, 6rem);
+  margin: 0 calc(50% - 50vw) 2rem;
+  padding: clamp(2.25rem, 5vw, 4rem) clamp(1.5rem, 8vw, 6rem);
   display: flex;
   flex-direction: column;
   align-items: center;
   justify-content: center;
-  gap: clamp(2.5rem, 7vh, 4.5rem);
+  gap: clamp(2rem, 5vh, 3.5rem);
   text-align: center;
-  min-height: max(540px, 100vh);
+  min-height: max(520px, 88vh);
   overflow: hidden;
   isolation: isolate;
 }
 
 .hero-media {
   position: fixed;
   inset: 0;
   width: 100vw;
   height: 100vh;
   z-index: -2;
   pointer-events: none;
   overflow: hidden;
 }
 
 .hero-media img,
 .hero-media video {
   position: absolute;
   inset: 0;
   width: 100%;
   height: 100%;
   object-fit: cover;
 }
 
-<<<<<<< HEAD
 .hero-media--static {
   background: linear-gradient(135deg, #0f172a 0%, #12463d 100%);
 }
 
 .hero-media--static .hero-video {
   display: none;
 }
-
-=======
->>>>>>> 89a0a6f65ac2e3cc4eeb44f5f397b11ea7d24b7c
 .hero-media--fallback .hero-video {
   display: none;
 }
 
 .page-filter {
   position: fixed;
   inset: 0;
   background: linear-gradient(160deg, rgba(0, 0, 0, .55) 0%, rgba(0, 0, 0, .9) 85%);
   pointer-events: none;
   z-index: -1;
 }
 
 :root.light .page-filter {
   background: linear-gradient(160deg, rgba(15, 23, 42, .35) 0%, rgba(15, 23, 42, .6) 85%);
 }
 
 .hero-video {
   display: block;
   filter: blur(var(--hero-blur));
   transform: scale(1.03);
   will-change: filter;
   z-index: 1;
 }
 
 .hero-image {
   display: block;
   filter: blur(var(--hero-blur));
   transform: scale(1.03);
 }
 
 .hero-panels {
   position: relative;
   display: flex;
   flex-direction: column;
   align-items: center;
   gap: clamp(1.5rem, 3vw, 2.75rem);
   width: 100%;
   max-width: min(960px, 100%);
-  margin-top: clamp(4rem, 15vh, 8rem);
+  margin-top: clamp(3rem, 10vh, 6rem);
   z-index: 1;
 }
 
 .hero-panel {
   background: linear-gradient(145deg, #1f8f73 0%, #1a6f62 100%);
   color: #f2f8f4;
   padding: clamp(1.75rem, 3vw, 2.5rem);
   border-radius: 1.5rem;
   box-shadow: 0 18px 45px rgba(0,0,0,.25);
   display: flex;
   flex-direction: column;
   gap: 1rem;
   align-items: flex-start;
   text-align: left;
 }
 
 .hero-panel h1,
 .hero-panel h2 {
   margin: 0;
   font-weight: 700;
   letter-spacing: .04em;
   text-transform: uppercase;
 }
 
 .hero-panel h1,
