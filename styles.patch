diff --git a/styles.css b/styles.css
index 07038ee1fa2900a6e49cfae05ed72645c6d6844f..ddad5547d174ab28423d01fc230bd06b72860144 100644
--- a/styles.css
+++ b/styles.css
@@ -311,152 +311,154 @@ body {
 #themeToggle:focus-visible {
   outline: none;
   box-shadow: var(--ring);
 }
 
 .simple-page {
   min-height: 70vh;
   max-width: 720px;
   margin: 0 auto;
   padding: clamp(2.5rem, 10vw, 5rem) 1.5rem;
   text-align: center;
   display: flex;
   flex-direction: column;
   gap: 1rem;
 }
 
 .simple-page p {
   color: var(--muted);
 }
 
 .simple-page a {
   color: var(--brand);
   font-weight: 600;
 }
 
-@media (max-width: 900px) {
-        :root {
+@media (max-width: 1300px) {
+  :root {
     --header-height: 210px;
   }
   .nav {
     flex-direction: column;
     align-items: stretch;
     padding: 1rem 1.25rem;
     row-gap: .5rem;
     position: relative;
   }
 
   .nav-top {
     display: flex;
   }
 
   .nav-inner {
     grid-template-columns: 1fr;
     justify-items: center;
     row-gap: 1rem;
   }
 
   .nav.nav--collapsible .nav-inner {
     display: none;
     width: 100%;
   }
 
   .nav.nav--collapsible.nav--open .nav-inner {
     display: grid;
     justify-items: start;
     text-align: left;
     align-items: start;
   }
 
   .nav-hamburger {
     display: inline-flex;
     align-self: center;
   }
 
   .nav-brand--mobile {
     display: flex;
     align-self: center;
   }
 
   .nav-inner > .brand {
     display: none;
   }
-  
+
   .nav.nav--collapsible .nav-links,
   .nav.nav--collapsible .nav-links--right {
     justify-content: flex-start;
   }
 
   .nav-links,
   .nav-links--right {
     justify-content: center;
     flex-direction: column;
     align-items: stretch;
     gap: 1rem;
     width: 100%;
   }
 
   .nav.nav--collapsible .nav-link,
   .nav.nav--collapsible .nav-toggle {
     justify-content: flex-start;
     width: 100%;
   }
   .nav.nav--collapsible .nav-link,
   .nav.nav--collapsible .nav-toggle,
   .nav.nav--collapsible .nav-menu-link,
   .nav.nav--collapsible .nav-menu-heading {
     font-size: 1.05rem;
   }
   .nav.nav--collapsible .nav-button {
     width: auto;
     align-self: flex-start;
   }
   .nav.nav--collapsible #themeToggle {
     width: auto;
     align-self: flex-start;
     display: inline-flex;
-	margin-left: 0;
+    margin-left: 0;
   }
-  
+
   .nav-item,
   .nav-item--nested {
     width: 100%;
   }
 
   .nav-menu {
     position: static;
     width: 100%;
     margin-top: .5rem;
     box-shadow: none;
     padding: .5rem 0;
     background: transparent;
   }
 
   .brand {
     margin: 0;
   }
+}
 
+@media (max-width: 900px) {
   .hero {
     margin: 0 calc(50% - 50vw) 2rem;
     padding: clamp(2rem, 10vw, 3.5rem) clamp(1rem, 7vw, 2.5rem);
     min-height: max(480px, 100vh);
     gap: clamp(2.5rem, 12vw, 4rem);
   }
 
   .hero-panels {
     margin-top: clamp(3rem, 14vh, 6rem);
     padding: 0 clamp(0.5rem, 4vw, 1.5rem);
     max-width: none;
   }
 }
 
 .hero {
   position: relative;
   margin: 0 calc(50% - 50vw) 3rem;
   padding: clamp(3rem, 6vw, 5rem) clamp(1.5rem, 8vw, 6rem);
   display: flex;
   flex-direction: column;
   align-items: center;
   justify-content: center;
   gap: clamp(2.5rem, 7vh, 4.5rem);
   text-align: center;
   min-height: max(540px, 100vh);
