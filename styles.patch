diff --git a/styles.css b/styles.css
index 2004fa2f619a58870f7016e7bac11702f1b1ab24..49b32987d69d54118c002161447fa490abb4065f 100644
--- a/styles.css
+++ b/styles.css
@@ -305,71 +305,81 @@ body {
   padding: clamp(2.5rem, 10vw, 5rem) 1.5rem;
   text-align: center;
   display: flex;
   flex-direction: column;
   gap: 1rem;
 }
 
 .simple-page p {
   color: var(--muted);
 }
 
 .simple-page a {
   color: var(--brand);
   font-weight: 600;
 }
 
 @media (max-width: 900px) {
         :root {
     --header-height: 210px;
   }
   .nav {
     flex-direction: column;
     align-items: stretch;
     padding: 1rem 1.25rem;
     row-gap: .5rem;
+    position: relative;
   }
 
   .nav-inner {
     grid-template-columns: 1fr;
     justify-items: center;
     row-gap: 1rem;
   }
 
   .nav.nav--collapsible .nav-inner {
     display: none;
+    width: 100%;
   }
 
   .nav.nav--collapsible.nav--open .nav-inner {
     display: grid;
+    justify-items: start;
+    text-align: left;
+    align-items: start;
   }
 
   .nav-hamburger {
     display: inline-flex;
     align-self: flex-end;
   }
 
+  .nav.nav--collapsible .nav-links,
+  .nav.nav--collapsible .nav-links--right {
+    justify-content: flex-start;
+  }
+
   .nav-links,
   .nav-links--right {
     justify-content: center;
     flex-wrap: wrap;
     gap: 1rem;
     width: 100%;
   }
 
   .nav-item,
   .nav-item--nested {
     width: 100%;
   }
 
   .nav-menu {
     position: static;
     width: 100%;
     margin-top: .5rem;
     box-shadow: none;
     padding: .5rem 0;
     background: transparent;
   }
 
   .brand {
     margin: 0;
   }
