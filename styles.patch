diff --git a/styles.css b/styles.css
index 8b78f7894c108e1d39db0a5cad2748b5475f5e03..97f60413371058e38e85713d1aa62bdcbe1b9196 100644
--- a/styles.css
+++ b/styles.css
@@ -960,50 +960,59 @@ body {
 }
 
 .pool-page p {
   font-size: 0.95rem;
 }
 
 .pool-page small,
 .pool-page .note {
   font-size: 0.8rem;
   color: var(--muted);
 }
 
 .pool-main {
   padding-top: clamp(6.5rem, 11vw, 9.5rem);
   max-width: min(1180px, 100% - 2.5rem);
   margin: 0 auto;
   width: 100%;
 }
 
 .pool-hero {
   max-width: min(1180px, 100% - 2.5rem);
   width: 100%;
   margin: 0 auto clamp(2.5rem, 5vw, 3.75rem);
 }
 
+.pool-hero-card {
+  background: color-mix(in srgb, var(--card) 88%, rgba(15, 23, 42, 0.12));
+  padding: clamp(1.75rem, 3.5vw, 2.25rem);
+}
+
+.pool-hero-card .pool-hero-inner {
+  align-items: start;
+}
+
 .pool-hero-inner {
   display: grid;
   grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
   align-items: center;
   gap: clamp(1.5rem, 4vw, 3rem);
 }
 
 .pool-hero-content {
   display: grid;
   gap: 0.75rem;
   align-content: start;
 }
 
 .eyebrow {
   text-transform: uppercase;
   letter-spacing: 0.18em;
   font-weight: 700;
   color: var(--brand);
   font-size: 0.9rem;
   margin: 0;
 }
 
 .pool-title {
   margin: 0;
   font-size: clamp(2.2rem, 5vw, 3rem);
