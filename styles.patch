diff --git a/styles.css b/styles.css
index abf339c74308fd5b7551c67812ebaba31e236fa7..9f93796ef183b6b235110f8de9879d532262e830 100644
--- a/styles.css
+++ b/styles.css
@@ -930,65 +930,70 @@ body {
     width: calc(100% - 1.5rem);
     border-radius: 1rem;
   }
 
   .footer-hours {
     flex-direction: column;
   }
 
   .footer-section-group::after {
     display: none;
   }
 
   .footer-section + .footer-section {
     border-top: 1px solid var(--footer-border);
     padding-top: 1.25rem;
     margin-top: 0.25rem;
   }
 
   .footer-meta {
     text-align: center;
   }
 }
 
 .pool-page {
   position: relative;
-  padding: clamp(2.5rem, 6vw, 4.5rem) 0 4.5rem;
+  padding: clamp(3rem, 6vw, 4.5rem) 0 clamp(5rem, 8vw, 6rem);
   display: flex;
   flex-direction: column;
-  gap: 2.5rem;
+  gap: clamp(2.75rem, 5vw, 3.75rem);
 }
 
 .pool-main {
-  padding-top: clamp(6rem, 12vw, 9rem);
+  padding-top: clamp(6.5rem, 11vw, 9.5rem);
+  max-width: min(1180px, 100% - 2.5rem);
+  margin: 0 auto;
+  width: 100%;
 }
 
 .pool-hero {
   display: grid;
   gap: 0.75rem;
   max-width: 960px;
   width: 100%;
+  margin: 0 auto clamp(2.5rem, 5vw, 3.75rem);
+  justify-items: center;
 }
 
 .pool-kicker {
   text-transform: uppercase;
   letter-spacing: 0.16em;
   font-weight: 700;
   color: var(--brand);
   font-size: 0.85rem;
 }
 
 .pool-lede {
   display: grid;
   gap: 0.75rem;
   color: var(--muted);
   line-height: 1.7;
 }
 
 .pool-hero-figure {
   margin: 0;
   border-radius: 1rem;
   overflow: hidden;
   box-shadow: 0 18px 48px rgba(0, 0, 0, 0.3);
 }
 
 .pool-hero-lightbox {
