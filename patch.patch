diff --git a/styles.css b/styles.css
index 0c726b1db1827a43afeedeba33afff07d3ab75d9..717aafb67a369ede5a6d1bac588f74283018ea30 100644
--- a/styles.css
+++ b/styles.css
@@ -1183,54 +1183,67 @@ body {
 .pool-hero-lightbox:focus-visible {
   outline: 3px solid(var(--brand));
   outline-offset: 4px;
 }
 
 .pool-hero-figure img {
   width: 100%;
   height: 100%;
   display: block;
   object-fit: cover;
 }
 
 .pool-section {
   background: var(--card);
   border: 1px solid rgba(148, 163, 184, 0.35);
   border-radius: 1.35rem;
   padding: clamp(1.75rem, 3vw, 2.5rem);
   display: grid;
   gap: 1.25rem;
   box-shadow: 0 18px 50px rgba(15, 23, 42, 0.28);
   max-width: min(1100px, 100% - 2.5rem);
   margin: 0 auto;
   scroll-margin-top: calc(var(--header-height) + 1.5rem);
 }
 
+.lodging-options {
+  display: grid;
+  gap: 1.25rem;
+  max-width: min(1100px, 100% - 2.5rem);
+  margin: 0 auto;
+  scroll-margin-top: calc(var(--header-height) + 1.5rem);
+}
+
 .pool-section + .pool-section {
   margin-top: clamp(1.75rem, 4vw, 3rem);
 }
 
+.lodging-options + .pool-section,
+.pool-section + .lodging-options {
+  margin-top: clamp(1.75rem, 4vw, 3rem);
+}
+
 .pool-section--two-column {
   grid-template-columns: repeat(2, minmax(0, 1fr));
   gap: clamp(1.25rem, 3vw, 1.75rem);
   align-items: start;
 }
 
 .section-header {
   display: grid;
   gap: 0.25rem;
 }
 
 .section-kicker {
   text-transform: uppercase;
   letter-spacing: 0.1em;
   font-weight: 700;
   color: var(--brand);
   font-size: 0.9rem;
   margin: 0;
 }
 
 .section-summary {
   margin: 0;
   color: var(--muted);
 }
 
