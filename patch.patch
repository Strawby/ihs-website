diff --git a/styles.css b/styles.css
index d5fdf374161a365c07fa2a036284f1c85725e62f..f65fb31d7316f4eed77d94cdcd90e329bb00ae9b 100644
--- a/styles.css
+++ b/styles.css
@@ -358,75 +358,75 @@ body {
   flex-direction: column;
   gap: clamp(2rem, 5vw, 3.5rem);
   text-align: left;
 }
 
 .history-hero {
   display: flex;
   flex-direction: column;
   gap: 0.75rem;
 }
 
 .history-hero .eyebrow {
   text-transform: uppercase;
   letter-spacing: 0.18em;
   font-weight: 700;
   color: var(--brand);
   font-size: 0.9rem;
   margin: 0;
 }
 
 .history-hero p {
   color: var(--muted);
 }
 
 .history-story {
-  display: grid;
+  display: flex;
+  flex-direction: column;
   gap: 1.5rem;
-  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
-  align-items: start;
   max-width: min(1100px, 100% - 2.5rem);
   margin: 0 auto;
 }
 
 .history-card {
   display: flex;
-  flex-direction: column;
-  gap: 1rem;
+  flex-direction: row;
+  gap: clamp(1rem, 3vw, 2rem);
   background: color-mix(in srgb, var(--card) 94%, var(--bg));
   border: 1px solid rgba(148, 163, 184, 0.35);
   border-radius: 1rem;
   padding: clamp(1.35rem, 3vw, 1.9rem);
   box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.03), 0 10px 28px rgba(15, 23, 42, 0.18);
-  height: 100%;
+  align-items: stretch;
 }
 
 .history-card img {
-  width: 100%;
+  width: clamp(220px, 35%, 360px);
   height: auto;
   display: block;
   border-radius: 16px;
+  object-fit: cover;
 }
 
 .history-card h2 {
   margin-bottom: 0.35rem;
 }
 
 .history-card p {
   color: var(--muted);
 }
 
 .history-timeline {
   display: flex;
   flex-direction: column;
   gap: 1.5rem;
 }
 
 .timeline-header p {
   color: var(--muted);
 }
 
 .timeline-scroll {
   max-height: 360px;
   overflow-y: auto;
   padding-right: 0.25rem;
 }
@@ -444,51 +444,59 @@ body {
   gap: 0.5rem;
   padding-bottom: 1.25rem;
   border-bottom: 1px solid rgba(255, 255, 255, 0.08);
 }
 
 .timeline-list li:last-child {
   border-bottom: none;
   padding-bottom: 0;
 }
 
 .timeline-year {
   font-weight: 700;
   color: var(--brand);
   letter-spacing: 0.08em;
   text-transform: uppercase;
   font-size: 0.85rem;
 }
 
 .timeline-list p {
   color: var(--muted);
   margin: 0;
 }
 
 @media (max-width: 900px) {
   .history-story {
-    grid-template-columns: 1fr;
+    gap: 1.25rem;
+  }
+
+  .history-card {
+    flex-direction: column;
+  }
+
+  .history-card img {
+    width: 100%;
   }
 
   .timeline-scroll {
     max-height: none;
   }
 }
 
 @media (max-width: 1300px) {
   :root {
     --header-height: 210px;
   }
   .nav {
     flex-direction: column;
     align-items: stretch;
     padding: 1rem 1.25rem;
     row-gap: .5rem;
     position: relative;
   }
 
   .nav-top {
     display: grid;
     grid-template-columns: 1fr auto 1fr;
     align-items: center;
   }
 
